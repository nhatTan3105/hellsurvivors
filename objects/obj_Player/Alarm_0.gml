/// @description Insert description here
// You can write your code in this editor

event_user(0);

alarm[0] = alarm_slash;
if (alarm[0] <= 0) {
    // Thực hiện hủy đối tượng ở đây
    instance_destroy();
}